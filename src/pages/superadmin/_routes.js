import {faHome} from "@fortawesome/free-solid-svg-icons";

const routes = [
  {
    url: "/dashboard",
    title: "routes.dashboard",
    icon: faHome,
    showInSideBar: true,
  },
];

export default routes;
